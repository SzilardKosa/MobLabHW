<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.moblabhw">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_studioghibli"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_studioghibli_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MobLabHW">
        <activity android:name=".view.details.DetailsActivity" android:theme="@style/Details"></activity>
        <activity android:name=".view.list.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>